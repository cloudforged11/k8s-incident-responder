version: "3.8"

services:
  incident-responder:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./incidents.db:/app/incidents.db
    restart: unless-stopped
